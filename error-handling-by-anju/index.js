try {
  console.log(age);
} catch (error) {
  console.error(
    `error name ${error.name} and error message is ${error.message}`
  );
}
var str = "very important works are here !";
console.log({ str });
